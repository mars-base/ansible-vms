version: 2
ethernets:
  {{ vm.nic_name | default('ens3') }}:
    addresses:
      - {{ vm.ip }}/{{ vm.netmask | default('22') }}
    routes:
      - to: default
        via: {{ vm.gateway }}
    nameservers:
      addresses:
{% for ns in vm.dns.split(',') %}
        - {{ ns | trim }}
{% endfor %}
